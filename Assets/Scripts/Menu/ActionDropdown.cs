using UnityEngine;
using TMPro;
using System.Collections.Generic;

public class ActionDropdown : MonoBehaviour
{
    //private TMP_Dropdown dropdown = null;

    //// Awake is called once before all other methods, when the GameObject is created
    //private void Awake()
    //{
    //    dropdown = GetComponent<TMP_Dropdown>();

    //    List<KeyboardActions> actionList = new List<KeyboardActions>();

    //    for (int i = 0; i < (int)(KeyboardActions.ACTION_NB); i++)
    //    {

    //    }

    //    dropdown.AddOptions(actionList);
    //}
}
